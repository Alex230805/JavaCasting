package Window;

import java.util.*;

public interface Input {
    /* input buffer used by the kayListener to keep track of the input */
    public static Set<Integer> inputBuffer = new HashSet<>();
}
